import React from 'react';
import { Tooltip } from '@mui/material';
import type { TooltipProps } from '@mui/material';

export const AuroraTooltip: React.FC<TooltipProps> = (props) => <Tooltip {...props} />;
