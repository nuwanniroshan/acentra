import React from 'react';
import { Rating } from '@mui/material';
import type { RatingProps } from '@mui/material';

export const AuroraRating: React.FC<RatingProps> = (props) => <Rating {...props} />;
